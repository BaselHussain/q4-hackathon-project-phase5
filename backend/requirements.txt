fastapi==0.109.0
sqlmodel>=0.0.22
asyncpg>=0.30.0
pydantic>=2.10.0
uvicorn[standard]==0.27.0
python-dotenv==1.0.0

# Authentication dependencies
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.1.3  # Pin to 4.x for passlib compatibility
email-validator>=2.1.0
slowapi>=0.1.9
python-multipart>=0.0.6

# MCP Server dependencies (Phase 3)
mcp>=1.0.0

# OpenAI Agents SDK (Phase 5 - AI Chat)
openai-agents>=0.0.7

# ChatKit Server SDK (Phase 6 - Frontend Chat Widget)
openai-chatkit>=0.1.0

# Event-Driven Architecture dependencies (Spec 8 - Kafka + Dapr)
httpx>=0.26.0
croniter>=2.0.0
websockets>=12.0
