# ============================================================================
# Task Management API - Environment Configuration
# ============================================================================
# Copy this file to .env and update with your actual values
# NEVER commit .env file to version control
# ============================================================================

# ----------------------------------------------------------------------------
# Database Configuration (REQUIRED)
# ----------------------------------------------------------------------------
# Neon Serverless PostgreSQL connection string
# Format: postgresql+asyncpg://username:password@hostname/database?sslmode=require
#
# Get your connection string from:
# 1. Log in to Neon Console (https://console.neon.tech)
# 2. Select your project
# 3. Go to "Connection Details"
# 4. Copy the connection string and add "+asyncpg" after "postgresql"
#
# Example:
# DATABASE_URL=postgresql+asyncpg://user:password@ep-cool-darkness-123456.us-east-2.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=postgresql+asyncpg://your_user:your_password@your_host.neon.tech/your_database?sslmode=require

# ----------------------------------------------------------------------------
# Application Environment (REQUIRED)
# ----------------------------------------------------------------------------
# Options: development, staging, production
# - development: Enables SQL query logging, detailed error messages
# - staging: Production-like environment for testing
# - production: Optimized for performance, minimal logging
ENVIRONMENT=development

# ----------------------------------------------------------------------------
# API Server Configuration (OPTIONAL)
# ----------------------------------------------------------------------------
# Host address to bind the server to
# Use 0.0.0.0 to accept connections from any network interface
# Use 127.0.0.1 for localhost only
# API_HOST=0.0.0.0

# Port number for the API server
# Default: 8000
# API_PORT=8000

# Enable auto-reload on code changes (development only)
# Set to true for development, false for production
# API_RELOAD=true

# ----------------------------------------------------------------------------
# Database Connection Pool Configuration (OPTIONAL)
# ----------------------------------------------------------------------------
# Maximum number of connections in the pool
# Default: 10
# Increase for high-concurrency applications
# DB_POOL_SIZE=10

# Maximum overflow connections beyond pool_size
# Default: 20
# DB_MAX_OVERFLOW=20

# Connection recycle time in seconds
# Connections older than this will be recycled
# Default: 3600 (1 hour)
# DB_POOL_RECYCLE=3600

# ----------------------------------------------------------------------------
# Logging Configuration (OPTIONAL)
# ----------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
# LOG_LEVEL=INFO

# Enable SQL query logging (development only)
# Set to true to see all SQL queries in logs
# Default: true in development, false in production
# SQL_ECHO=true

# ----------------------------------------------------------------------------
# CORS Configuration (OPTIONAL)
# ----------------------------------------------------------------------------
# Allowed origins for CORS (comma-separated)
# Use "*" to allow all origins (development only)
# In production, specify exact origins
# Example: CORS_ORIGINS=https://example.com,https://app.example.com
# CORS_ORIGINS=*

# ----------------------------------------------------------------------------
# Authentication & Security Configuration (REQUIRED for auth features)
# ----------------------------------------------------------------------------
# Better Auth shared secret key for JWT token signing and validation
#
# ⚠️  CRITICAL SECURITY REQUIREMENT ⚠️
# - MUST be at least 32 characters long
# - MUST be cryptographically random (use one of the methods below)
# - MUST be different for development, staging, and production
# - MUST be kept secret (never commit to version control, never share)
# - SHOULD be rotated periodically for security
#
# 🔐 HOW TO GENERATE A SECURE SECRET:
#
# Method 1: Using Python (recommended)
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
#   Example output: xK9mP2vN8qR5tY7wZ3aB6cD1eF4gH0jL2mN5pQ8rS
#
# Method 2: Using OpenSSL
#   openssl rand -base64 32
#   Example output: 7Xk9mP2vN8qR5tY7wZ3aB6cD1eF4gH0jL2mN5pQ8=
#
# Method 3: Using /dev/urandom (Linux/Mac)
#   head -c 32 /dev/urandom | base64
#   Example output: 3Bk7mP1vN6qR4tY5wZ2aB5cD0eF3gH9jL1mN4pQ7=
#
# Method 4: Using PowerShell (Windows)
#   -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
#
# ⚠️  DO NOT USE:
# - Simple passwords or passphrases
# - Dictionary words or common phrases
# - Predictable patterns (e.g., "12345678901234567890123456789012")
# - The example value below (it's just a placeholder!)
#
# After generating, replace the value below with your generated secret:
BETTER_AUTH_SECRET=REPLACE_THIS_WITH_YOUR_GENERATED_SECRET_MIN_32_CHARS

# JWT token expiration time in hours
# Default: 24 (24 hours)
# Shorter expiration = more secure but users need to login more often
# Longer expiration = more convenient but tokens valid longer if compromised
# TOKEN_EXPIRE_HOURS=24

# JWT signing algorithm
# Default: HS256 (HMAC with SHA-256)
# ⚠️  DO NOT CHANGE unless you know what you're doing
# ALGORITHM=HS256

# ----------------------------------------------------------------------------
# Rate Limiting Configuration (OPTIONAL)
# ----------------------------------------------------------------------------
# Enable rate limiting for authentication endpoints
# Protects against brute force attacks
# Default: true
# RATE_LIMIT_ENABLED=true

# Maximum login attempts per minute per IP address
# Default: 5 requests per minute
# Lower = more secure but may affect legitimate users
# Higher = more convenient but easier to brute force
# RATE_LIMIT_LOGIN_ATTEMPTS=5

# ----------------------------------------------------------------------------
# Notes
# ----------------------------------------------------------------------------
# 1. DATABASE_URL is REQUIRED - the application will not start without it
# 2. All other variables are optional and have sensible defaults
# 3. For production deployment, review and configure all security settings
# 4. Keep .env file secure and never commit it to version control
# 5. Use different .env files for different environments (dev, staging, prod)
# ============================================================================
